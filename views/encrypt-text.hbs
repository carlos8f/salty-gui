<p class="pull-right">
  <a class="btn btn-primary" href="/encrypt?to={{to}}"><i class="glyphicon glyphicon-lock"></i> Encrypt</a> <a class="btn btn-default" href="/decrypt"><i class="glyphicon glyphicon-eye-open"></i> Decrypt</a><br><a class="btn btn-primary" href="/encrypt/text?to={{to}}"><i class="glyphicon glyphicon-copy"></i> Copy/paste</a> <a class="btn btn-default" href="/encrypt/upload"><i class="glyphicon glyphicon-upload"></i> Upload</a>
</p>

<h2>Encrypt</h2>

<h3>Here you can paste or enter text to encrypt.</h3>

<p>You will receive an ASCII-armored encrypted result to copy/paste.</p>

<hr>

<div id="encrypt-text">
  <form method="post" action="">
    <div class="form-group">
      <label for="recipient"><i class="glyphicon glyphicon-user"></i> Recipient:</label>
      <select name="to" class="form-control">
        <option value="{{pubkey.pubkey}}">(Self)</option>
        {{#each recipients}}
        <option value="{{pubkey}}"{{#if selected}} selected{{/if}}>{{name}} &lt;{{email}}&gt;</option>
        {{/each}}
      </select>
    </div>
    <div class="checkbox">
      <label>
        <input type="checkbox" name="sign" value="1"{{#if post.sign}} checked{{/if}}>
        <i class="glyphicon glyphicon-pencil"></i> Include signature
      </label>
    </div>

    <div id="actions" class="row">
      <div class="col-lg-9">
        <div class="form-group">
          <label for="input"><i class="glyphicon glyphicon-envelope"></i> Enter text:</label>
          <textarea id="input" class="form-control" name="input" rows="8">{{post.input}}</textarea>
        </div>
        <button type="submit" class="btn btn-success"><i class="glyphicon glyphicon-ok"></i> Encrypt</button>
        {{#if output}}
        <hr>
        <div class="form-group">
          <label for="input"><span class="text-success"><i class="glyphicon glyphicon-check"></i> Output:</span></label>
          <textarea id="output" class="form-control" name="output" rows="8" readonly onclick="this.select()">{{output}}</textarea>
        </div>
        {{/if}}
      </div>
    </div>
  </form>
</div>